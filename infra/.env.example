# compose 变量（RS-02a / D-Q68，2026-07-27）。复制为 infra/.env 后填**强随机值**。
#
#   cp infra/.env.example infra/.env
#
# 本文件是 compose 的插值来源。compose 按「compose 文件所在目录」找 `.env`，故
# `infra/.env` 无需任何 --env-file 参数，从仓库根、infra/ 内、或任意目录带绝对路径
# 跑都能取到（2026-07-27 实测三种 cwd）。
# infra/.env 已被 .gitignore 挡住，**不要 commit**。
#
# ⚠️ 口令会被拼进 DSN（`postgresql://user:PASSWORD@host/db`），**必须 URL 安全**。
# 一律用 hex 生成，别手敲——`@ : / ? # %` 任一个进了口令都会把 DSN 解析歪，
# 而症状是「连不上」而不是「口令错」，很费时间：
#
#   openssl rand -hex 32            # Linux / macOS / Git-Bash
#   python -c "import secrets;print(secrets.token_hex(32))"   # 跨平台

# ── PostgreSQL ────────────────────────────────────────────────────────────────
# 超管口令。只在**空数据卷首次 initdb** 时生效；库已经建好了改这里不会真的改密，
# 必须在运行库上 ALTER ROLE——见 .agent/evidence/RS-02a/deploy-rotate-secrets.md。
POSTGRES_PASSWORD=

# 三业务角色（同上：只在首次 initdb 时被 pg-init/02-roles.sh 读取）
ERP_APP_DB_PASSWORD=
ERP_MIGRATOR_DB_PASSWORD=
PORTAL_APP_DB_PASSWORD=

# ── Redis ─────────────────────────────────────────────────────────────────────
# requirepass。无状态，随时可改，改完 `make up` 重建 redis + api + beat 即可。
REDIS_PASSWORD=

# ── 应用密钥 ──────────────────────────────────────────────────────────────────
# JWT 签名密钥。换掉＝所有人当前的 access/refresh token 立即失效、需重新登录，
# 除此之外无副作用。
ERP_JWT_SECRET=

# 凭证加密密钥（pgcrypto 对称密钥）。
#
# 🔴 **换这一个跟换其他的不一样**：app.store_credential.client_secret_encrypted 与
# app.proxy.password_encrypted 是用它加密的。直接改成新值，既有密文就再也解不开
# ——全部店铺的 Walmart client_secret 与代理口令一起变砖，渠道调用全线失败。
#
# 已有数据的库要换它，必须走重加密：
#   python -m erp.tools.rotate_credential_key --old <旧值> --new <新值>
# 该工具在一个事务里解旧密→加新密，并当场回读验证；详见工具头注。
ERP_CREDENTIAL_KEY=

# ── 运行档 ────────────────────────────────────────────────────────────────────
# 不设即 prod（compose 写的是 ${ERP_ENV:-prod}）。**这是有意的**：prod 下
# ERP_ALLOW_INSECURE_DEFAULTS 无效，任何弱密钥都拒绝启动、没有绕过口子。
# 只有确实要在本机跑开发档时才取消注释——那会重新打开放行开关。
# ERP_ENV=dev

# Swagger UI（http://<本机>:8000/api/docs）。默认关——该端口内网可达而此页无鉴权。
# **要接口文档就改这一个**，不要去改上面的 ERP_ENV：那两件事已经拆开了
# （审查 AI 的 N1），改 ERP_ENV 会连带把弱密钥放行开关重新打开。
# ERP_DOCS_ENABLED=true

# ── 采集 worker（profile: scraper，默认不起）─────────────────────────────────
# ERP_WORKER_ENROLL_TOKEN=
# PROXY_URL=
# ERP_WORKER_NODE_KEY=erp-scraper-1
