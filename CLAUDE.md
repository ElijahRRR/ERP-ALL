# ERP-ALL 项目指南（Agent 必读）

## 协作分工（四方，先认清自己是谁）

> 与 `specs/007-mvp-completion-plan/README.md`「角色分工」节同源（Owner 2026-07-26 新增
> 独立审查 AI，主文由审计侧落笔于 `90c7ffd`；写权边界于 `df17ca5` 收紧对齐既成实践）。
> **本表若与 007 冲突，以 007 为准。**

| 角色 | 职责 | 边界 |
|---|---|---|
| **云端 AI** | 写代码、写 `.agent/` 台账、开 PR、写批注回传 | 代码 + `.agent/` + **其余 specs（含 002 契约：openapi 随端点同 PR 维护）**；**不写** `specs/007-*` 与 `specs/001-domain-model/` 图纸正文——那两处只可**追加「已落地」实现附注**，设计变更走**批注回传**（如 `.agent/evidence/R2-09/owner-rulings-*.md`） |
| **部署 AI**（Win11 部署机本地） | 真机验证、数据迁移、定时任务、取证 | **不改码、不 push**。给它的指令必须可整段粘贴、自带铁律（绝不 `pg_restore` 进 erp_all；暂存用一次性容器且用毕删容器+匿名卷；不输出密钥） |
| **审查 AI**（独立评审，2026-07-26 新设） | **逐 PR 通读实际 diff** 出审查报告 | **只读**——审查结论**必须发成 PR 评论**（`.agent/evidence/reviews/PR-<n>.md` 是可选附件，**不能只写文件不发评论**：只写文件对方收不到唤醒，报告等于没交）；**不写** specs、不写 `review_list.json`、不改代码、不 push 分支 |
| **规划/审查 AI**（审计侧） | 规划、`specs/007-*` 与图纸正文落笔、跨域架构、验收判定、给 Owner 打包决策 | 云端侧的批注由它落笔；它有权 block |

**合并前闸序（2026-07-26 起）**：CI 绿 → **审查 AI 通读 diff** → 部署机真机验证 →
**Owner 授权合并**。审查 AI 位于 Owner 拍板**之前**，其价值即保护该次授权决策。
**云端 AI 不得自写自己 PR 的审查报告**——独立性正是这条闸的用意（它设立的直接由头就是
云端侧两次 PR 正文失实：一次写「零代码改动」、一次写「零迁移」而实际含迁移，
而 Owner 的合并授权正是基于正文）。

**Owner 只做三类事**：拍决策、提供只有人能给的东西（凭证/授权/固定 IP/异地备份）、授权合并。
凡是「上机操作」派给部署 AI、「通读 diff 出审查」派给审查 AI、「改 007/图纸」派给规划审查
AI——**不要默认丢回 Owner**。

## 自动循环协议（Owner 2026-07-27 批准；这是行为规则，不含项目状态）

各方**自驱运转、不等 Owner 催**，但循环有明确的刹车。**本节只写规则；当前在推什么、
开着哪些 PR 一律现查，不写进文档**（状态快照会过时，过时的底稿比没有底稿更危险）。

| 角色 | 怎么被唤醒 | 醒了做什么 |
|---|---|---|
| 云端 AI | 自己 PR 的评论 / CI 事件（订阅推送）+ 长间隔兜底自检 | 处理审查意见与 CI 失败；无待办则推进队列里下一张工单 |
| 审查 AI | **定时轮询**开着的 PR（订阅是按 PR 挂的，没有「订阅整个仓库」这种能力） | 审出新提交 → 贴评论 → 订阅该 PR 接后续 → 重排下次轮询 |
| 规划/审查 AI | 按需 + 低频 | 看全局漂移（工单账、图纸符合性、队列缺口），**不逐 PR 盯 CI** |
| 部署 AI | Owner 转交指令 | 真机验证与取证 |

**「审过没有」的判据**：审查 AI 在评论里写明所审的 commit sha；下次轮询比对 head 是否
已变，避免重复审同一版或漏审新推的提交。

### 事件流拉平（2026-07-28 定，让循环真正自转）

**为什么两侧唤醒速度天生不对称**：订阅**按 PR 挂，没有仓库级订阅**；且 webhook
**不可靠投递**的恰好是三类——CI 成功、**新推送**、合并冲突状态变化。于是：
云端侧的触发器是「有人评论 / CI 失败」（**可靠送达**）→ 秒醒；
审查侧的触发器是「出现新 PR / 有人推了新提交」（**推不过来**）→ 只能靠轮询。
**这不是配置差别，是信号可推送性的差别**，靠调轮询频率解决不了，只会烧 token。

三条规则把它拉平：

1. **云端侧：每次推完待审提交，在 PR 上贴一条「待审，本次请审 `<sha>`」评论。**
   这把不可靠的「新推送」转换成可靠的「评论」事件——只要审查侧已订阅该 PR 即秒醒。
   > ⚠️ **不是 @提及，也无法 @提及**：四方 AI 都通过 Owner 的 GitHub 账号操作，
   > 审查 AI **没有独立 GitHub 身份**可被 @。唤醒**完全由订阅决定**——已订阅该 PR 的
   > 会话会收到该 PR 上的**任何**评论，与内容、与 @谁无关（本会话实证：部署机那条
   > 补验评论同样出自 Owner 账号，照样唤醒了订阅方）。**推论：对审查侧尚未发现、
   > 因而未订阅的全新 PR，贴评论没有任何唤醒作用**——那种情况只能等它轮询发现。
   附带收益：PR 上留下「何时请求审查、审的是哪一版」的公开记录，与「正文只写不变量」
   互补（审查针对哪个 sha 有据可查，不依赖会漂的正文描述）。
2. **审查侧：拿到任何 PR——自己轮询发现的、或 Owner 手动指派的——第一步先订阅，再开审。**
   **订阅提到审之前**，否则审的过程中对方推了新提交或回了话，那段空窗收不到。
   「读 PR」不等于「订阅 PR」，订阅是一次独立调用，不会因为读过就自动生效。
3. **订阅绑在会话上，不跨会话继承。** 审查侧（及任何订阅方）**每次新开会话的第一件事**
   ＝列出当前所有开着的 PR 并逐个订阅，否则等于失联。

**拉平后**：只剩「发现一张全新 PR」依赖轮询，其余全事件驱动。发现这一步本就不急
（新 PR 从创建到 CI 跑完也要几分钟），**轮询间隔 30–60 分钟足够**，不要更短。

**推论一：双向都必须留评论，沉默不是信号。** 可靠送达的只有「评论」与「CI 失败」；
**「新推送」与「CI 成功」都不推送**。所以：
- 云端侧推完待审提交 → 贴「待审，本次请审 `<sha>`」（否则审查侧静默）；
- **审查侧出完结论 → 必须贴评论**（否则云端侧静默）。写进 `.agent/evidence/reviews/`
  的文件**不产生任何唤醒**，它只是留档；**只写文件不发评论 = 报告没交**。
- 任何一方结束回合时若指望对方接手，**必须留评论**——把"我做完了/轮到你了"变成事件。

**推论二：等 CI 转绿的一方必须自己去查。** 「CI 成功」不推送，干等永远等不到：
推完代码后一路绿，对订阅方是**完全静默**的。谁在等绿谁就排一次短延时自检去查
（`get_check_runs`，几分钟量级），**不要把"没收到消息"当成"还没跑完"**。
反过来 CI **失败**会推送，所以"没消息"更可能是绿了或还在跑，而不是红了。

**审查侧轮询醒来只做三件事**（Owner 2026-07-28 定形；其余一律靠事件唤醒）：

1. 列出当前开着的 PR；
2. **未订阅的 → 订阅**；
3. **head sha ≠ 自己上次审过的 sha → 审掉**，并在评论里写明本次所审 sha。

做完即停，不做别的。第 3 条不可省——**省掉它，每张新 PR 的首审永远不会发生**：
云端侧贴「待审」评论的时刻，审查侧尚未发现该 PR、尚未订阅，那条评论**射进空气**，
此后再无新事件来触发首审，PR 会静静躺着无人审。第 3 条同时兜住第二个漏——
云端侧推了代码却忘贴评论（新推送本就不可靠投递）。

### 必须停下来问 Owner（升级清单，从严）

**凡涉及以下任一，立即停并推送给 Owner，不得为了「继续推进」自行拍板**：
业务口径与语义 · 范围增减（加功能/砍功能/改验收判据）· **花钱**（采购、LLM 预算、云资源）
· **删数据或不可逆操作** · 与 `specs/000-founding/` 宪法冲突 · 安全边界放宽 ·
对外暴露（公网/门户/第三方）· 需要凭证或授权。

**可自走不必问**：修 bug、补测试、改内部实现、按已有验收判据推进已立工单、处理 CI 失败、
按审查意见做等价修复。**判断不准时一律按"停"处理**——停的代价是等，走错的代价是返工或损失。

### 刹车与治理

1. **合并授权永远在人手里。** 循环跑到「等 Owner 合并」就停——开发与审查同为 Claude，
   两边达成一致也可能一起错（本项目已有互相纠错的实例），**人工合并闸是唯一的第三方**。
   不得为追求全自动去掉这道闸，也不得自行合并自己的 PR。
2. **云端 AI 不得自写自己 PR 的审查报告**（独立性即本闸全部价值）。
3. **空转治理**：能事件驱动就不轮询；轮询只作长间隔兜底；**队列空了就停，不要空转**。
   每次唤醒都是真实开销。
4. **需要决策时主动推送 Owner**，别静默卡住——「停下来等」不等于「没人知道」。
5. **PR 正文只写不变量，不写会漂的快照数字。** 截至 2026-07-28 已发生 **5 次正文失实**
   （「纯台账回写零代码改动」实含代码、「零迁移」实含迁移、commit 表停在旧数、
   「10 个文件」实为 12…），且开发侧两次自我纠正做法后仍复发——**说明靠自律修不好**。
   规则：正文**不写**文件数 / commit 数 / 测试条数这类随 head 变动的计数；要写就写
   「本 PR 的全部代码变更 = `git diff <sha>~1 <sha> -- backend/`」这类**可复算的不变量**，
   或明确标注「截至 sha X」。**Owner 的合并授权基于正文，正文失实即授权失据**；
   审查 AI 的第一条纪律（以源码为准、先核对声称范围 == 实际 diff）正是为此设的兜底。
6. **状态在仓库不在会话**：`.agent/` 台账、`specs/`、工单验收判据是唯一事实源。
   会话被压缩/重启/换人都不影响接续——**这是本循环得以成立的前提，任何时候都不要
   把关键状态只留在会话上下文里**。

## 铁律

1. **specs/000-founding/ 是宪法**：DECISION-FORM.md（50 项决策）> PRD-v1.md > business-rules-ledger.md。任何实现与之冲突：改代码或经 Owner 批准改文档，不许静默偏离。
2. **workflow 模式**：任务状态在 `.agent/`（task/review_list/progress/handoff）。每个工单一个原子目标；产出必须回写工单状态；无工单的改动不合并。
3. **角色制**：`.claude/agents/` 定义六角色的管辖目录与禁区。跨界需求提单给对应 owner，不许顺手改。
4. **验证纪律**（D-Q37 稳定优先）：每个增量必须 CI 绿 + dry-run 证据（渠道写路径）+ A152 实测/影子对拍 才算完成。测试验收店 = A152。
5. **禁区**：不写死业务参数（一律配置中心）；不引入 SQLite 到生产路径；不绕过 walmart_client 直连渠道 API；凭证只走加密存储。

## 关键决策速查

- 多租户：资源默认 team_id 隔离，共享开关仅超管（D-Q30）；去重键 (team_id, asin) + 店铺豁免（D-Q31）
- SKU：master_sku=M{seq} 渠道中立；渠道 SKU 默认=master_sku（D1/D-Q2）
- listing 单管道双模式 offer_mode ∈ {build, match}；定价策略按模式分套（D-Q3/23）
- 采购执行双入口：内部权限点 + 外部隔离门户（D-Q50）
- 自动化三档：人工/半自动/全自动，流程级开关（D-Q13/29）

## 前端规范（008）

前端改动（新页面/新组件/契约变更）先读 `specs/008-frontend-conventions/README.md`：
数据层纪律（统一 client + codegen 类型，禁手写响应 interface）、新增功能上车
六项清单、对账可见性底线。审计侧按该清单查前端 PR，违例记 FE-DEBT 台账。
