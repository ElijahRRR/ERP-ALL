# Task Definition
- Mode: build (R0 done → R0-tail design → R1 walking skeleton)
- Task: Build ERP-ALL per specs/000-founding/ (PRD-v1 accepted; 50 decisions locked)
- Constraints: workflow discipline per CLAUDE.md; one atomic objective per loop; verification evidence required; A152 is the only live-test store; live writes gated by Owner.
- Acceptance: each iteration lands with CI green + evidence + review_list updated.
