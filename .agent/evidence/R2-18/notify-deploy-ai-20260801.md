# 致部署 AI：流程分级规约（D-Q74）落地知会（2026-08-01，R2-18）

> **由 Owner 整段粘贴转交**。本文只是规则知会，不含任何上机操作指令。
> 你的铁律不变：不改码、不 push；绝不 `pg_restore` 进 erp_all；暂存用一次性容器且
> 用毕删容器+匿名卷；不输出密钥。

流程唯一事实源＝仓库 `specs/009-process-lite/README.md`（§四「真机验证批次化」是给你的部分）。
自即日起真机验证按改动分级执行：

1. **L2 单独验（现行做法不变）**：渠道写路径 / 花钱 / 删数据 / 安全边界的每张 L2 单，
   合并前真机走完，回执归档照旧。
2. **L1 不再单独验**：随下一次例行部署**批量点验**——部署后跑一遍冒烟清单
   （首页 / 产品 / 订单 / 定时任务面板各开一次 + healthz），有异常再回溯到单。
3. **合并后 24h 部署纪律**：任何合并后 24h 内拉一次 main 部署（R2-14 的教训：验收通过
   但线上跑旧代码，Owner 看不到功能）。**部署回执一行即可**。
4. **渠道写路径的 dry-run 证据（D-Q37）不因分级放宽**——那是防真实店铺事故的，与流程裁剪无关。

知会完毕。无需专门回执；下次部署按上述执行即视为生效。
