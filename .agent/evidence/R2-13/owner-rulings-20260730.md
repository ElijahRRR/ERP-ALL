# Owner 裁定回传（2026-07-30）—— R2-13 四条待裁定 + R2-15 相关 + 一条我的错误更正

> **本文性质**：Owner 口头裁定的落仓记录 + **需审计侧改图纸正文的清单**。
> 云端侧不写 `specs/001-domain-model/` 与 `specs/007-*` 正文，故 §一 的图纸改动
> **必须由规划/审查 AI（审计侧）执行**，本文只提请。
> 裁定原文出自 Owner 2026-07-30 回复（PR #47 合并同一轮）。

---

## 一、`daily_cap` 权威方 —— **裁定：账号属性唯一权威，自动化策略不配这个值**

**Owner 原话**：「自动化策略不应该配置这个值，一切以帐号自己配置的属性为准，规划搞复杂了。」

我提的三个口径（列为权威 / 护栏键为权威 / 取较小值）**均未被采纳**，Owner 选的是更简的第四种：
**护栏键直接不存在。**

### 处置

| 项 | 结论 |
|---|---|
| `buyer_account.daily_cap` INT 列（`07-order-sourcing-aftersale.md:144`） | **唯一权威**，保留，13b 按它路由 |
| `automation_policy.config.daily_cap` 护栏键 | **取消**——不进护栏键清单，13c 不实现它 |

### ⚠️ 需审计侧改的图纸正文（云端侧不可写）

1. **`specs/001-domain-model/09-platform.md:208`** —— `purchase_execute` 行的护栏键列
   现为「amount_ceiling（单单金额上限）、**daily_cap（单账号日采购上限）**、price_delta_pct」，
   **应删掉 `daily_cap`**，留两键。
2. **`specs/007-mvp-completion-plan/README.md:280`** —— 同句同处，同样删 `daily_cap`。
   （`007:278` 那句「按站点+可用性+`daily_cap` 路由」**保留不动**——那说的正是账号列，
   是权威侧，与本裁定一致。）

### 对工单的影响

- **13c 护栏从三键降为两键**：`amount_ceiling` + `price_delta_pct`。工作量略减。
- **13b 不变**：仍建 `buyer_account.daily_cap` 列并按它路由。
- **消失的那个失效模式**：原先「两处都能设、一处设了另一处照旧放行」的出口，
  因护栏侧不存在而**从根上没有了**——这比我提的「取较小值」更彻底：
  取较小值仍要求两处都被正确查询，而「只有一处」连查漏的可能都不存在。

> **记一笔我的判断偏差**：我推荐取较小值，理由是「列属账号风控、护栏属策略信任度，
> 是两个问题」。Owner 的判断是这在本项目里不是两个问题——账号日限就是账号日限，
> 引入第二处只是给自己造对齐负担。**这条我认同**：我的方案在「概念上更完备」，
> 但完备性本身不是收益，它要拿「两处必须永远一致」的维护成本去换。

---

## 二、gate 口径 —— **裁定：接受**

**Owner 原话**：「能，把全自动先放着，接受『13a/13b/13d 先做，全自动开关锁在 13c 交付并验收之后』。」

- 13a / 13b / 13d 可立即开工（不需等 R2-09 收账）；
- **全自动档开关锁死在 13c 交付并验收之后**；
- 13e（灰度切换）仍在 13c 验收②之后。

> 附带修正：007 字面「排在 R2-09 之后」其实**留了个洞**——R2-09 从未建护栏消费点
> （那本是 13c 的内容），照字面 R2-09 一收账就可开 auto。本裁定把闸挪到 13c 之后，
> 是**更严**而非放宽。此点建议审计侧在 007 的 R2-13 排期句上加一句附注。

---

## 三、两个路径前缀 —— **裁定：把插件改成一组**（推翻我的推荐）

**Owner 原话**：「把插件改成一组，符合我们这边的实际情况。」

我推荐的是「ERP 侧两组都实现、不改插件路径」，理由是保住 13e 的「改 baseUrl 即回切」。
**Owner 选择改插件为一组。已按裁定执行，不再申辩。**

### 但有一个下游后果必须写进 13e，否则它的判据会失真

原先 13e 的回切设想是「把 baseUrl 指回厂商 SaaS」。**插件路径改成一组之后这条不成立**——
fork 版插件的 URL 路径已与厂商后端不一致，指回去会 404。

→ **13e 的回切步骤必须写成「换回厂商原版插件」，而不是「改 baseUrl」**，且判据要覆盖：
   - 回切演练必须实测「装回原版插件 + 原 baseUrl」这条路，不能只测改配置；
   - 红线不变：**同一浏览器配置内绝不同时启用两个插件**（会重复下单）。

这不是对裁定的异议，是裁定引出的、必须落到 13e 判据里的事实。**建议审计侧在 007 的
13e 描述里把回切方式明确成「换插件」**。

---

## 四、`buyer_session` / cookies —— **裁定：要，用途是识别买家号**；细节待 Owner 分析结果

**Owner 原话**：「要把 cookie 给服务端，其中某个参数是用于识别买家号。你说要做一整套
生命周期，不必吧？要 cookies 的目的在我看来就只是为了识别买家号哪个是哪个，订单分配
执行不会错。我稍后会把网站给本地 ai 分析一下，等我具体结果。」

### 已定

- **保留 `cookies` 权限与 `updateBuyerCookie`**（我原推荐「删掉」作废）；
- 用途限定为**识别买家号身份**，不是「让服务端以买家身份行动」。

### 我对「不必做整套生命周期」的回应：**用途限定成识别，这话就成立**

我原先说要做加密/轮换/过期/审计，前提是**把整份 cookie 存下来长期使用**。
若用途只是识别，那正确做法是：

> **服务端收到后当场提取出那个识别字段，只存这个识别值（或其哈希），
> 原始 cookie jar 一律不落库。**

这样一来没有任何长期敏感物，加密/轮换/过期**确实都不需要**——Owner 的判断成立，
是我把「存整份」当成了默认前提。

### 但有一条已核实的事实可能会改变方案，请 Owner 在分析时一并考虑

插件现在的调用是：

```js
ApiService.updateBuyerCookie(customerId, country, cookieJson)
```

**`customerId` 是一个与 cookie 并列的独立参数**，不是从 cookie 里解出来的。
即厂商侧「这是哪个买家号」这件事**已经由 `customerId` 承载**，cookie jar 是另作他用
（其形态——全字段带 value 的完整 jar——更像是给服务端复用会话准备的）。

→ 所以「取 cookie 是为了识别买家号」这个前提**可能与插件实际设计不符**。三种可能：
   1. `customerId` 是我们 ERP 侧的买家号主键，cookie 用来反查「这个浏览器当前登录的是
      哪个亚马逊账号」，用于**校验二者是否对得上**（防止把订单派给登错号的浏览器）
      —— 若是这个用途，那它确实是识别，且很有价值；
   2. cookie 真是给服务端复用会话的（那用途就不止识别，生命周期问题回来了）；
   3. 历史遗留，实际没被消费。

**请 Owner 的本地分析顺带确认：厂商后端拿这份 cookie 做了什么。** 这决定 13a 是
「提取一个识别值就丢弃」还是「要当凭证管」。**在此结论出来之前，13a 不动 cookie 相关代码。**

### 与其余安全项的关系（不受本裁定影响）

- 删 `storage` / `tabs` / `scripting` 三条零调用权限：**照做**（与 cookies 无关）；
- 收窄 `host_permissions` 到 amazon 三站：**照做**，且已复核**零行为变更**
  （`getAll` 的 url 来自调用方所在页，实际只读到 amazon 自己的 cookie）；
- 给 `chrome.runtime.onMessage` 加 `sender` 校验：**照做，且因保留 cookies 而更必要**
  —— 该监听器是「按传入 URL 读任意域 cookie」的原语，保留 cookies 权限意味着这个原语
  继续存在，`host_permissions` 收窄与 `sender` 校验是它剩下的两道边界。

---

## 五、订单回连「多条命中取最新」—— **我错了，此条作废**

我在 2026-07-29 提出：`order/pull.py` 的 `ORDER BY l.id DESC LIMIT 1` 在 R2-15 引入
`-2` 后缀后，「同店同 ASIN 多条 listing」会从例外变成常态，故「取最新」将升级为
承重业务规则、需要口径。

**Owner 指出一个 ASIN 只对应一条产品链接，我说的多条其实是历史记录。Owner 是对的，
而且理由比这更硬：**

```
0009_listing.py:104   CONSTRAINT uq_listing UNIQUE (store_id, channel_sku)   ← 无 WHERE，无部分索引
order/pull.py:172     WHERE l.store_id = :store AND l.channel_sku = :sku     ← 与唯一键逐字相同
```

**查询条件与唯一约束的列完全一致 ⇒ 至多命中一行，这是数据库层面的保证，不是业务约定。**
`ORDER BY l.id DESC LIMIT 1` 是永远不会被用到的防御代码——它无法「消歧」，因为不存在
可消歧的第二行。

而且后缀机制本身就保证了这点：加 `-2` 的目的正是让每条 listing 的 `channel_sku` 互不相同，
于是「同店同 ASIN 的多条 listing」各自持有**不同的** `channel_sku`（`ASIN` / `ASIN-2`…），
订单按成交时的那个 SKU 来，各自精确命中一行。

**我的错误在于**：我看到「唯一约束不带状态过滤 ⇒ 旧 listing 照样占着 SKU」，就推出
「会有多条同 ASIN 的 listing」——这步对；但接着把它当成「查询会命中多条」——**这步错了**，
因为那些行的 `channel_sku` 恰恰是被唯一约束逼成互不相同的。**同一个约束我用对了一半、
用错了另一半。**

→ **R2-15 因此少一件事**：不需要为「多条命中」定业务口径。

> 但**真正的风险在另一个方向，且已由审计侧写进新判据⑦**：那两个子查询
> **落空时返回 NULL 而 INSERT 仍成功**，订单静默失去产品关联。零命中才是问题，
> 多命中不是。审计侧抓的这条比我提的那条切中要害。

---

## 六、工作方式 —— **裁定：非阻塞落档攒起来**

**Owner 原话**：「你自己工作方式的攒起来。」

已执行：
- 审查侧判「可签」之后**不再为非阻塞内容追加 commit**；
- 非阻塞的考古/落档一律攒到**对应工单开工时**一次做完；
- 例外仅两种：审查侧提出的**需修项**，与 **Owner 指示**。

（成因记录：PR #47 后三轮审查全由「已判可签后我又追加内容」触发，末两轮只找出文档内部
一致性问题、无实质缺陷——审查轮次是真实成本，而那些内容早一天晚一天进仓对 13a 无差别。）

---

## 附：本文提请审计侧执行的图纸改动汇总（云端侧不可写）

| # | 文件 | 改什么 | 依据 |
|---|---|---|---|
| 1 | `001-domain-model/09-platform.md:208` | `purchase_execute` 护栏键删 `daily_cap`，留两键 | §一 |
| 2 | `007-mvp-completion-plan/README.md:280` | 同上 | §一 |
| 3 | `007-mvp-completion-plan/README.md`（R2-13 排期句） | 加附注：闸从「排 R2-09 之后」精确为「auto 档排 13c 验收之后」 | §二 |
| 4 | `007-mvp-completion-plan/README.md`（13e 描述） | 回切方式明确为「换回厂商原版插件」，非「改 baseUrl」 | §三 |

> 第 1、2 项**必须在 13b/13c 动工前落笔**；第 3、4 项在 13e 动工前即可。
>
> ⚠️ **本表不是全部**：同日晚 Owner 又下了四条裁定，其提请事项见下方「补记」——
> 尤其 **补-4（`stop_before_payment` 档位 + 验收分两层）** 与
> **补-5（`daily_cap`：审计侧 `a8568d3` 的修法与 Owner 裁定相反，需先确认口径）**。
> 补-5 **取代**本表第 1、2 项：那两处已被 `a8568d3` 改动，但改成了 Owner 否掉的方案。

---

# 补记（同日晚，Owner 四条新裁定 + 一条与图纸的冲突）

## 补-1. cookies：**推翻本文 §四**，改为「不收，删掉」

**Owner 原话**：「先不收 cookie，删掉 cookies 权限和 updateBuyerCookie」。

### 为什么同一天会反转——因为查清了那份 cookie 到底是什么

本文 §四 记的裁定是「要，用途是识别买家号」。Owner 追问「`updateBuyerCookie` 是做什么用的」，
逐字读 `background.js` 的字段整形后得到答案：

```js
{ domain, expirationDate, hostOnly, httpOnly, name, path,
  sameSite: cookie.sameSite || 'no_restriction',
  secure, session, storeId: cookie.storeId || '1', value, id: index + 1 }
```

`hostOnly` / `sameSite:'no_restriction'` / `storeId:'1'` / `id: index+1` 这一组字段，
**就是 Cookie-Editor（EditThisCookie）那类插件的 JSON 导出格式**——那个格式存在的唯一目的
是**被导入回浏览器**。所以它不是「一个标识」，是**整套可搬运的登录态**。

**用途因此是会话搬运/复用**（让服务端或另一个浏览器以该买家身份行动，例如浏览器关闭后
仍能查订单状态），**不是识别**。识别由同一调用里的**独立参数** `customerId` 承载：

```js
ApiService.updateBuyerCookie(customerId, country, cookieJson)
```

即：**原裁定的目的（识别）由 `customerId` 达成，cookie jar 与该目的无关。** 目的既已另有
达成路径，收 cookie 就只剩下风险没有收益——那份 jar 一旦落库，持有者即可以买家身份下单。

→ 处置：`cookies` 权限、`updateBuyerCookie`、`getCookiesAsJson()`、`background.js` 的
`onMessage` 监听器**全部删除**。fork 后 `permissions` 应为空数组（`chrome.runtime` 无需声明）。
图纸 `buyer_session` 表**不建**；其「启用条件」（需服务端在浏览器关闭时查订单状态 /
需集中监测会话健康）留作将来真出现该需求时的判据。

> **附带作废一条 13a 动作**：此前建议「给 `onMessage` 加 `sender` 校验」——整个监听器都删了，
> 校验失去对象。**删掉比「加校验 + 收窄 host_permissions」更强。**

## 补-2. `customerId` 的录入通道：**保留提取按钮**（推翻一手来源 §7 那条）

**Owner 原话**：「提取 customerId 按钮需要，我们预配置账号 ID 也是用插件提取出来、然后
配置进去的，我一开始并不知道我的 ID 在哪里看」。

一手来源 §7 写「提取 customerId 按钮——我们预配置账号 ID，**不需要**」。**该条错**：
预配置的值本身来自那个按钮。**不是「不需要它」，是「在开账号阶段需要它一次」。**

→ 这同时填上了本文此前报的输入缺口「数十个买家账号的 `external_customer_id` 从哪来」：
**靠插件提取、人工录入**。故 13b 必须保留这条录入通道，且它是**一次性配置工具**而非运行时功能。

> **优先级规则据此精确化**（提请审计侧落笔）：一手来源对「厂商实际怎么做」是权威，
> 对「我们该怎么做」的**推荐不是权威**。§7 已出现两处推荐失准（本条 + `asinCheck` 前置化），
> 后者若照做会直接撞验收⑦。

## 补-3. 日本站：**MVP 暂不做** ⇒ fork v2.4.1 即可

**Owner 原话**：「暂时不需要日本站 47 都道府县映射」。

→ 本文此前报的「fork v2.4.1 还是 v2.5.0」**不再是缺口**：v2.5.0 的核心新增就是
`JP_PREFECTURE_EN_TO_JA`，JP 不做则 v2.4.1 够用，仓里现有版本可直接 fork。
**但 `receivingDistrict`（JP 必填）与异常类 `address` 的 JP 分支仍应保留代码路径**——
不做不等于删掉，将来开 JP 时不必回头补结构，只需补映射表。

## 补-4. 验收①②⑤ 的执行方式：**先停在付款前一步**

**Owner 原话**：「测试买家账号 + 花钱额度到时候前面停在最后一步付款就可以，然后前面通了
我会拿真实订单来测完整流程，并不浪费钱」。

这条解开了「验收要真花钱」的死结，但**引出一个图纸里没有的必需能力**：

> **插件 fork 必须带一个「走到付款页即停并回报」的档位。** 否则「停在付款前一步」无法执行
> ——现插件的流程是一路走到下单完成。

建议沿用本仓既有的 `channel.gateway_mode` 同款三态思路，给采购链一个独立开关：

| 档 | 行为 | 用途 |
|---|---|---|
| `dry_run` | 只做校验与地址/购物车填充，**不进结账页** | CI 与本地 |
| `stop_before_payment` | 走完结账页、抓到实付金额与预计送达，**在点付款前停下并回报** | **验收①②⑤ 的主力档** |
| `live` | 完整下单 | Owner 用真实订单收口 |

`stop_before_payment` 档同时能验到几件原本以为必须花钱才能验的东西：**实付金额（价格护栏
三段式的客户端判定所需）、预计送达（`delivery_days_limit`）、非 FBA / bundle 前置拦截**。
即验收②⑥⑦⑧ 都可在不花钱的前提下证伪。

→ **提请审计侧把这个档位写进 §07 与工单**，并把验收分成两层：`stop_before_payment` 可证伪的
（②⑥⑦⑧⑨③④）与必须 `live` 的（①⑤ 的最后一步）。

## 补-5. ⚠️ `daily_cap`：审计侧 `a8568d3` 的图纸修法**与 Owner 裁定相反**，需 Owner 定夺

本文 §一 记录的 Owner 裁定是：

> 「自动化策略**不应该**配置这个值，一切以帐号自己配置的属性为准，**规划搞复杂了**」

即三个候选口径（列为权威 / 护栏键为权威 / **取较小值**）中，**取较小值被明确否掉**。

而审计侧 `a8568d3` 落笔的是：

```
09-platform.md:208   daily_cap（**团队策略上限**；与 `buyer_account.daily_cap` 账号硬上限
                     **取更严者**生效，见 §07 护栏表）
```

**「取更严者」就是被否掉的那个取较小值方案。** 推测成因：本文（含 §一 的裁定）还在**未合并的
PR #48** 里，审计侧修图纸时大概率没读到，只看到「三处口径不一致」这个现象。

**附带出现第二处不一致**：`007:280` 仍写「`daily_cap` 账号日限」（账号唯一权威的读法），
与 `09:208` 的「团队策略上限 + 取更严者」互相矛盾。故现在是**三种口径并存**：

| 出处 | 口径 |
|---|---|
| Owner 2026-07-30 裁定 | 账号列唯一权威，策略不配 |
| `09-platform.md:208`（a8568d3） | 两层，取更严者 |
| `007:280` | 账号日限（＝账号权威） |

→ **13b/13c 动工前必须定死一个。** 云端侧不自行选择，也不在此复述利弊（Owner 已就此表过态）。
**建议**：把 §一 那份裁定原文转给审计侧，由其按裁定改回；若 Owner 改主意采纳两层方案，
则本文 §一 需同步更正——两种情况都要求**先有一次明确的口径确认**。
