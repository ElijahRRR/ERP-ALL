# R2-07 07a 验收①：A152 真机对账（2026-07-17）

- 部署机 HEAD：176da4cf3186b94017f70c4af7ec0ef4a6d11199（PR #19 合并点）
- Alembic：0031 (head)，migrate 退出码 0
- A152：store_id=1（is_test）；API 健康
- 执行命令：`docker compose -f infra/docker-compose.yml exec api python -m erp.tools.return_pull_verify --store 1 --pull-first`

## 完整输出

```text
2026-07-17 04:33:59 [info] gateway.token_refreshed store=A152test
return_pull 先行：{'returns': 36, 'lines': 36, 'events': 0, 'pages': 1}
渠道 RMA 数 36 / DB RMA 数 36
对账一致 ✅
```

## 判读

- 真实渠道数据：A152 有 36 张真实退货单（1 页拉完，36 头 36 行）；
  首轮拉取 events=0 属预期（首次入库=行本身即快照，不记变更事件）。
- 对账口径：RMA 集合 / 头级聚合状态 / 金额±0.01 / 行数 / 行级退款状态，
  与 `map_return` 共用映射（口径不分叉）——全部一致。
- 部署侧声明：未手工 DDL、未改网关配置、未输出凭证；本机无 GNU make，
  按 Makefile `up` 配方原样执行 compose（与手册回退指令一致）。

**结论：R2-07 验收①（A152 真实 returns 拉取与渠道对账一致）通过。07a 整片收账。**
